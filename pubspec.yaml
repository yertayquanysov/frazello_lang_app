name: frazello
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  flutter_bloc: ^8.1.6
  dio: ^5.7.0
  just_audio: ^0.9.41
  google_fonts: ^6.2.1
  logger: ^2.4.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true